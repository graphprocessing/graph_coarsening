language: cpp

matrix:
  include:
    - os: linux
      dist: trusty
      script: python build.py build gcc
    - os: osx
      osx_image: xcode7.2
      script: python build.py build clang
    - os: windows
      script: python build.py build msvc
